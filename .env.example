# Parola pentru /admin (schimb-o în producție!)
ADMIN_PASSWORD=margarite2026

# Secret pentru cookie-ul de sesiune (folosește un text lung, aleator)
ADMIN_SESSION_SECRET=schimba-acest-secret-lung-aleator

# Pe Vercel: Storage → Blob → creează store și copiază token-ul
# Fără acest token, anunțurile se salvează doar local (util la dezvoltare)
# BLOB_READ_WRITE_TOKEN=
